<%= form_with(model: ceo, local: true) do |form| %>
  <% if ceo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ceo.errors.count, "error") %> prohibited this ceo from being saved:</h2>

      <ul>
      <% ceo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login_id %>
    <%= form.text_field :login_id %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
